# Tortopedia — контент-план

## Завершено

### Как хранить фигурки из мастики

Статус: опубликовано.

RU:

```text
https://tortopedia.in.ua/kak-hranit-figurki-iz-mastiki/
```

UA:

```text
https://tortopedia.in.ua/uk/yak-zberigaty-fihurky-z-mastyky/
```

Этап завершён:

- статья опубликована;
- RU/UA версии готовы;
- внутренние ссылки добавлены;
- sitemap обновлён;
- URL ранее отправлены в Google Search Console.

---

### Как сушить фигурки из мастики

Статус: опубликовано.

RU:

```text
https://tortopedia.in.ua/kak-sushit-figurki-iz-mastiki/
```

UA:

```text
https://tortopedia.in.ua/uk/yak-sushyty-fihurky-z-mastyky/
```

Основной интент:

```text
как сушить фигурки из мастики
```

Сильный подинтент:

```text
как быстро высушить фигурки из мастики
```

Этап завершён:

- RU/UA статьи опубликованы;
- внутренняя перелинковка добавлена;
- XML sitemap обновлён;
- HTML sitemap обновлён;
- статья попадает в автоматический блок последних статей;
- статья выводится в блоке новых материалов на главной.

Логика кластера:

```text
лепка → сушка → хранение
```

---

### Главная страница: блок новых статей

Статус: реализовано.

На RU и UA главных работает автоматический блок:

```text
Новые статьи
Нові статті
```

Он выводит 6 последних материалов из коллекций `recentRu` / `recentUk`.

Старый статический блок популярных рецептов удалён, чтобы не перегружать главную двумя похожими сетками карточек.

Live проверен пользователем — всё корректно.

---

## Текущий кластер

### Мужские торты: общий хаб + мастер-классы

После публикации материалов по цепочке `лепка → сушка → хранение` контентный backlog пересмотрен заново по старому семантическому ядру, актуальному Keyword Planner, данным GSC, SERP и существующим страницам Tortopedia.

Текущий самостоятельный кластер: `украшение торта для мужчины`.

### 1. Общая статья-хаб

Статус: опубликовано.

Глобальный `post_id`: 1043.

RU:

```text
https://tortopedia.in.ua/kak-ukrasit-tort-dlya-muzhchiny/
```

UA:

```text
https://tortopedia.in.ua/uk/yak-prykrasyty-tort-dlya-cholovika/
```

Основной интент:

```text
как украсить торт для мужчины в домашних условиях
```

Материал опубликован как общий хаб по оформлению мужского торта и разведён по интенту с существующим пошаговым мастер-классом:

```text
/kak-ukrasit-tort-na-den-rozhdeniya-muzhu/
```

В статье раскрыты:

- идеи оформления мужского торта;
- оформление своими руками;
- варианты без мастики;
- крем;
- шоколад;
- сладости;
- топперы;
- надписи;
- тематическое оформление;
- варианты на день рождения и юбилей.

Существующий мастер-класс со смокингом используется как отдельный дочерний материал и связан внутренней перелинковкой.

Статья добавлена в новую категорию:

```text
Идеи и вдохновение
Ідеї та натхнення
```

Категория опубликована в RU/UA версиях и подключена в основную навигацию.

Этап завершён:

- RU/UA версии опубликованы;
- новая категория создана;
- категория подключена в меню;
- внутренняя перелинковка добавлена;
- XML sitemap обновлён;
- HTML sitemap обновлён;
- `llms.txt` обновлён;
- изображения добавлены;
- production build выполнен;
- commit/push выполнен.

Commit:

```text
37a20c3 Add men's cake decoration guide and ideas category
```

Keyword Planner подтвердил актуальный спрос:

- `торт на день рождение мужчине` — 100–1 тыс.;
- `как украсить торт для мужчины в домашних условиях` — 10–100;
- `оформление торта для мужчины` — 10–100;
- `торт для мужчины без мастики` — 10–100;
- `торт для мужчины своими руками` — 10–100;
- `торт для мужа на день рождения` — 10–100;
- `торт мужчине на юбилей` — 10–100.

GSC показывал, что существующий мастер-класс уже ранжировался по широким запросам, поэтому новая общая статья была чётко разведена с ним по интенту и связана внутренней перелинковкой.

### 2. Следующий мастер-класс

Планируемый глобальный `post_id`: 1044.

Рабочая тема:

```text
Торт «Футбольное поле»: как сделать своими руками
```

Keyword Planner:

- `торт футбольное поле` — 100–1 тыс.;
- `торт футболисту` — 100–1 тыс.;
- `торт для футболиста` — 100–1 тыс.;
- `как сделать торт футбольное поле` — 10–100;
- `торт футбольное поле своими руками` — 10–100;
- `торт футбольное поле из мастики` — 10–100.

На Tortopedia отдельного материала по футбольному торту сейчас нет.

Перед созданием материала повторно проверить SERP, каннибализацию и структуру статьи.

### 3. Следующий кандидат

Планируемый глобальный `post_id`: 1045.

Рабочая тема:

```text
Торт для рыбака
```

Keyword Planner:

- `торт для рыбака` — 100–1 тыс.;
- `торт рыбаку на день рождения` — 10–100;
- `торт рыбалка` — 10–100;
- `торт на тему рыбалки` — 10–100;
- `торт для рыбака своими руками` — 10–100;
- `как украсить торт для рыбака` — 10–100;
- `оформление торта для рыбака` — 10–100;
- `торт рыбаку без мастики` — 10–100.

Предварительный порядок дальнейшего развития кластера:

```text
общий хаб → футбольный торт → торт для рыбака
```

Перед созданием каждого материала повторно проверить SERP, каннибализацию и структуру статьи.

### Отвердители и добавки для мастики

Отдельный обзор `CMC / Tylose / Gum-Tex / трагакант` сейчас не развивать.

Причина: после публикации материалов про CMC и мастику для лепки фигурок дальнейшее расширение этого направления создаёт риск смыслового пересечения и каннибализации.

Вернуться к теме только при подтверждённом самостоятельном поисковом интенте.

---

## Глоссарий терминов

Цель глоссария — не SEO само по себе, а помощь новичку в понимании материалов Tortopedia.

Если читатель встречает незнакомый термин вроде `топпер`, `КМЦ`, `молд`, `вайнер`, `айсинг` и т. п., он должен быстро понять его значение и продолжить чтение без необходимости искать объяснение на других сайтах.

Глоссарий должен соответствовать общему позиционированию Tortopedia:

> практический справочник по домашнему декору тортов для человека без профессионального кондитерского опыта.

### Базовая структура

Предусмотреть отдельную страницу-глоссарий со списком основных терминов.

Для каждого термина использовать простое объяснение без профессионального перегруза:

1. Что это.
2. Для чего используется.
3. Где встречается в работе с тортами.
4. При необходимости — чем отличается от похожих понятий.
5. Иллюстрация, если термин сложно понять только по тексту.

### Связь со статьями

Термины должны быть связаны с основными материалами Tortopedia внутренними ссылками.

Если в статье появляется понятие, которое начинающий читатель может не знать, желательно дать возможность быстро перейти к его объяснению.

При этом не превращать статьи в энциклопедию и не ставить ссылки на очевидные общеупотребительные слова.

### Отдельные страницы терминов

Не создавать отдельную страницу для каждого слова только ради количества страниц или SEO.

Короткие и простые определения можно оставить внутри общей страницы глоссария.

Для важных терминов, которые требуют более подробного объяснения, допускаются:

- отдельные короткие страницы;
- расширенные карточки;
- ссылки на существующие полноценные статьи Tortopedia.

Отдельная страница оправдана, если одного короткого определения уже недостаточно для понимания термина.

### Первый набор терминов

Начать примерно с 20–30 базовых понятий, которые чаще всего встречаются в материалах Tortopedia.

Предварительный список:

- мастика;
- мастика для обтяжки;
- мастика для лепки;
- КМЦ;
- Gum-Tex;
- Tylose;
- трагакант;
- топпер;
- молд;
- вайнер;
- айсинг;
- ганаш;
- крем-чиз;
- кондитерский мешок;
- насадка;
- шпатель;
- скребок;
- поворотный столик;
- подложка для торта;
- пищевой краситель;
- гелевый краситель;
- кандурин;
- изомальт;
- вафельная бумага;
- сахарная бумага;
- съедобная печать;
- армирование фигурки;
- конденсат на мастике;
- выравнивание торта;
- обтяжка торта.

Список не считать окончательным. Пополнять глоссарий по мере появления новых статей и терминов, которые требуют пояснения новичку.

### Принцип развития

Глоссарий должен развиваться вслед за основным контентом, а не отдельно от него.

При подготовке каждой новой статьи проверять:

- появились ли новые профессиональные термины;
- понятны ли они человеку без кондитерского опыта;
- есть ли термин уже в глоссарии;
- нужно ли добавить определение или внутреннюю ссылку.

Глоссарий должен уменьшать порог входа в материалы Tortopedia, а не становиться самостоятельным SEO-каталогом терминов.

### Интерактивные подсказки в статьях

Принято решение дополнить глоссарий контекстными всплывающими подсказками непосредственно внутри статей.

Логика:

- при первом появлении потенциально непонятного термина в статье читатель может открыть короткое пояснение;
- на десктопе подсказка должна работать по наведению и/или клику;
- на мобильных устройствах — по тапу;
- подсказка содержит короткое определение на 1–3 предложения;
- для сложных терминов внутри подсказки может быть ссылка `Подробнее` на глоссарий, расширенную карточку или отдельный материал;
- повторные употребления того же термина в одной статье оставлять обычным текстом, чтобы не перегружать страницу.

Определения не дублировать вручную в каждой статье.

Создать единый централизованный источник терминов с RU/UA-версиями определений. Статьи должны обращаться к нему по идентификатору термина, например:

```text
topper
kmc
mold
veiner
```

Это позволит:

- исправлять определение в одном месте;
- автоматически использовать одинаковое объяснение во всех статьях;
- поддерживать симметрию RU/UA;
- в дальнейшем автоматически формировать из тех же данных страницу общего глоссария.

Важно: хинты должны помогать чтению, а не превращать текст в набор всплывающих элементов. Добавлять их только к действительно профессиональным или потенциально непонятным новичку терминам.

---

## Структурированные данные и микроразметка

Текущее состояние:

- системной JSON-LD-разметки для всего сайта пока нет;
- для старого рецепта мастики из маршмеллоу используется отдельная Schema.org-разметка типа `Recipe` через Microdata (`itemscope` / `itemprop`);
- эта разметка подключается только для конкретного материала с `post_id: 645`;
- старую Recipe-разметку не тиражировать на новые материалы без ревизии.

План развития:

- добавить базовую JSON-LD-разметку для статей (`Article` или `BlogPosting`);
- добавить `BreadcrumbList` для статей и категорий;
- добавить `WebSite` для Tortopedia;
- при необходимости добавить сущность издателя/бренда сайта (`Organization`);
- использовать `Recipe` только для материалов, которые действительно являются рецептами;
- поддерживать RU/UA данные симметрично;
- формировать структурированные данные из front matter и общих шаблонов, а не вручную в каждой статье.

Перед внедрением отдельно проверить старую разметку рецепта маршмеллоу.

Особое внимание:

- устаревший скрытый Microdata-блок;
- `aggregateRating` / `reviewCount`, если для них нет надёжного фактического источника;
- старый Flash/`object` для видео;
- корректность `author`, `datePublished`, `image`, `recipeIngredient`, `recipeInstructions`;
- соответствие текущим требованиям Google к structured data.

Цель — сделать микроразметку системной частью архитектуры Tortopedia, а не набором точечных вставок в отдельных материалах.

---

## Технические контентные задачи

Блок последних статей и главная автоматизированы через:

```text
recentRu
recentUk
```

При публикации новых материалов достаточно корректно указывать `published` и `lang`; свежая статья автоматически вытеснит самую старую из текущей шестёрки.

---

После завершения текущего кластера снова актуализировать этот файл и пересмотреть backlog по фактическим данным поиска.
