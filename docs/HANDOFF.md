# HANDOFF.md

Дата: 2026-09-21

## 1. Проект

Tortopedia — статический RU/UA сайт о мастике и декоре тортов.

Локальный репозиторий:

```text
D:\Git\tortopedia
```

GitHub:

```text
saitkharkov-cyber/tortopedia
```

Branch:

```text
main
```

Архитектура:

```text
src/
-> Eleventy 3.1.6
-> _site-pilot/
-> publish.ps1
-> production HTML в корне
-> GitHub main
-> Cloudflare Pages
-> https://tortopedia.in.ua/
```

Основное правило:

```text
редактировать src/
не править production HTML вручную
```

---

## 2. С чего начать следующую сессию

Сначала прочитать:

```text
docs/START_SESSION.md
docs/CHATGPT_WORKFLOW_RULES.md
docs/CURRENT_SESSION_STATE.md
docs/HANDOFF.md
docs/ARTICLE_PRODUCTION_REGULATION.md
docs/CONTENT_PLAN.md
docs/CONTENT_MAP.md
```

Затем проверить Git:

```powershell
git status --short && git log --oneline --decorate -7
```

Фактическое состояние Git всегда приоритетнее документации.

Последний известный remote HEAD на момент завершения сессии:

```text
864a86d Finalize current session state
```

Перед любыми новыми правками сверить local HEAD и remote.

---

## 3. Что завершено 2026-09-21

### Точечные исправления

Исправлена UA-формулировка в статье о хранении торта:

```text
Як правильно дістати торт із холодильника
→
Як правильно вийняти торт із холодильника
```

Commit:

```text
b8ac82d Fix Ukrainian cake storage heading
```

В RU-статье «Как сушить фигурки из мастики» повтор hero заменён на отдельное изображение вентилятора.

Commit:

```text
b4d63e3 Fix fan image in fondant drying guide
```

---

## 4. Новый контентный кластер

Старый backlog пересмотрен заново по:

- старому семантическому ядру;
- актуальному Keyword Planner;
- данным GSC;
- SERP;
- существующим страницам Tortopedia;
- риску каннибализации.

Выбран кластер:

```text
украшение торта для мужчины
```

### План ближайших материалов

```text
1043 — общий хаб: «Как украсить торт для мужчины в домашних условиях»
1044 — МК: «Торт „Футбольное поле“: как сделать своими руками»
1045 — МК: «Торт для рыбака»
```

Существующая страница:

```text
/kak-ukrasit-tort-na-den-rozhdeniya-muzhu/
```

остаётся отдельным узким мастер-классом по торту-смокингу из мастики.

Новый материал 1043 должен быть общей статьёй-хабом и ссылаться на этот МК как на один из вариантов, не дублируя его.

---

## 5. Что подтвердили данные

### GSC по существующему мужскому МК

Страница уже получает показы по широким запросам, в том числе:

```text
как украсить торт для мужчины в домашних условиях
как украсить торт для мужчины
как украсить торт для мужа
как украсить торт мужчине на день рождения
```

Это подтверждает необходимость аккуратно развести интенты:

```text
общий хаб ≠ конкретный МК «смокинг»
```

### Keyword Planner

Общий мужской кластер имеет актуальный спрос.

Сильные тематические направления:

```text
торт футбольное поле — 100–1 тыс.
торт футболисту — 100–1 тыс.
торт для футболиста — 100–1 тыс.
торт для рыбака — 100–1 тыс.
```

Уточняющие запросы для футбольной и рыболовной тем в основном находятся в диапазоне 10–100.

Паспортный подкластер Planner как самостоятельный устойчивый кластер не подтвердил; пока не приоритизировать.

---

## 6. Документация обновлена

Обновлены:

```text
docs/CONTENT_PLAN.md
docs/CONTENT_MAP.md
```

Коммиты:

```text
0601c7b Update content plan for men's cake cluster
cdd54d9 Update content map for men's cake cluster
```

Они теперь отражают:

- новый мужской кластер;
- порядок 1043 → 1044 → 1045;
- роль существующего МК со смокингом;
- необходимость повторной проверки SERP/каннибализации перед каждой статьёй.

---

## 7. Семантическое ядро

Рабочий формат теперь только один:

```text
docs/TORTOPEDIA-Semanticheskoe-YADRO.txt
```

Файл:

- UTF-8;
- собран из всех листов старого Excel;
- очищен от trailing whitespace;
- проверен через `git diff --cached --check`;
- пригоден для поиска и анализа прямо из репозитория.

Commit добавления:

```text
03e2617 Add text export of semantic core
```

Excel-файл удалён как дублирующий:

```text
d294fc2 Remove obsolete Excel semantic core
```

Промежуточная папка:

```text
docs/semantic_parts/
```

удалена как техническая и в Git не добавлялась.

---

## 8. Следующий глобальный post_id

Текущий использованный максимум:

```text
1042
```

Следующий свободный ID:

```text
1043
```

Один материал = один глобальный post_id, общий для RU и UA.

Правило:

```text
следующий ID = max + 1
```

---

## 9. Следующая статья

Следующая новая статья:

**Как украсить торт для мужчины в домашних условиях**

Это должен быть общий hub-материал, а не пошаговый МК одного дизайна.

Перед написанием:

1. проверить актуальный SERP;
2. уточнить структуру и интенты;
3. проверить каннибализацию с существующим МК со смокингом;
4. утвердить H1 / Title / Description;
5. следовать `docs/ARTICLE_PRODUCTION_REGULATION.md`;
6. только после утверждения структуры начинать текст.

Статья 1043 ещё не начата.

---

## 10. Дальнейший порядок

После 1043:

```text
1044 — торт «Футбольное поле»
1045 — торт для рыбака
```

Перед каждым материалом повторно проверить:

- SERP;
- семантику;
- существующие страницы;
- внутреннюю перелинковку;
- риск каннибализации.

Не продолжать старый backlog автоматически.

---

## 11. Что пока не развивать

Отдельный обзор:

```text
CMC / Tylose / Gum-Tex / трагакант
```

сейчас не развивать без нового самостоятельного интента.

Причина: высокий риск смыслового пересечения с уже опубликованными материалами про КМЦ и мастику для лепки фигурок.

---

## 12. Рабочие правила

Пользователь предпочитает:

- один следующий шаг за раз;
- `++` / `+++` = предыдущий шаг выполнен, дать только следующий;
- PowerShell-команды одной строкой;
- `git add` одной строкой через `&&`;
- сначала проверить факты, затем менять;
- не делать scope creep;
- не commit/push без явного подтверждения;
- RU и UA проверять отдельно;
- перед commit смотреть staged diff;
- использовать `git diff --check`.

Публикация:

```text
npm run build
-> проверить _site-pilot
-> .\publish.ps1
-> git diff
-> git diff --check
-> git add
-> staged diff/stat
-> commit
-> push
-> production check
```

---

## 13. Итог дня

За 2026-09-21 закрыты:

- две точечные правки контента;
- пересмотр старого backlog;
- выбор нового кластера мужских тортов;
- фиксация плана 1043 → 1044 → 1045;
- обновление CONTENT_PLAN.md;
- обновление CONTENT_MAP.md;
- перенос семантического ядра в UTF-8 TXT;
- удаление промежуточных semantic_parts;
- удаление устаревшего Excel-дубликата;
- обновление CURRENT_SESSION_STATE.md;
- подготовка этого HANDOFF.

Точка продолжения следующей сессии:

```text
проверить git status / git log
→ прочитать CURRENT_SESSION_STATE + HANDOFF
→ начать с SERP и структуры статьи 1043
```
