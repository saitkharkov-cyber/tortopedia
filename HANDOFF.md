# HANDOFF — Tortopedia

Дата: 2026-09-03
Ветка: `main`

## Текущее состояние

Техническая миграция сайта завершена. Production работает на Cloudflare Pages и после последнего push проверен вручную — обновление применилось корректно.

Сайт больше не поддерживается как набор вручную редактируемых HTML-файлов. Источник истины теперь — Eleventy-исходники в `src`.

Текущая схема:

`src + layouts/partials → npm run build → _site-pilot → publish.ps1 → готовые HTML в корне → GitHub main → Cloudflare Pages`

Cloudflare пока НЕ собирает Eleventy сам. Это сделано намеренно: сборка и проверка выполняются локально.

## Eleventy

- Eleventy: 3.1.6
- исходники: `src`
- build output: `_site-pilot`
- команда сборки: `npm run build`
- успешная контрольная сборка: 54 HTML-файла
- `_site-pilot/`, `node_modules/`, `backup/` игнорируются Git

Все 54 production-страницы перенесены в Eleventy:

- 40 статей: 20 RU + 20 UA
- 6 обычных страниц: `about`, `mastic-decoration`, `html-sitemap` RU/UA
- 6 страниц категорий, включая `/page/2/`
- 2 главные страницы RU/UA

Все типы страниц были сравнены со старым статическим HTML. Подтверждено 54/54.

Допустимые различия при сравнении:

- cache-buster `ms-icon-144x144.png?v=...`
- динамический `userSettings.time`
- несущественные пробелы/переносы
- ранее проверенные эквивалентные self-links

## Шаблоны

Основные layouts:

- `src/_includes/layouts/article.njk`
- `src/_includes/layouts/article-uk.njk`
- `src/_includes/layouts/page.njk`
- `src/_includes/layouts/page-uk.njk`
- `src/_includes/layouts/category.njk`
- `src/_includes/layouts/category-uk.njk`
- `src/_includes/layouts/category-page2.njk`
- `src/_includes/layouts/category-page2-uk.njk`
- `src/_includes/layouts/home.njk`
- `src/_includes/layouts/home-uk.njk`

Уникальная Recipe-разметка статьи `post_id == 645` сохранена отдельными partials:

- `src/_includes/partials/recipe-schema-marshmallow.njk`
- `src/_includes/partials/recipe-schema-marshmallow-uk.njk`

## Публикация

В корне репозитория есть `publish.ps1`.

Он:

1. запускает `npm run build`;
2. требует ровно 54 HTML-файла;
3. копирует собранные HTML из `_site-pilot` в production-структуру корня;
4. запускает `git diff --check`;
5. показывает `git diff --stat`.

Рабочий цикл теперь:

1. редактировать только `src` и шаблоны;
2. выполнить `./publish.ps1`;
3. посмотреть diff;
4. commit;
5. push в `main`;
6. при важных изменениях проверить production.

Не редактировать готовые HTML в корне как основной источник — они являются результатом сборки.

## Важные коммиты этапа

- `8c724f0` — `Complete Eleventy site migration`
- `13a01a9` — merge `pilot-11ty` в `main`
- `6f4fad5` — публикация Eleventy-generated HTML
- `65f71a4` — `Add local publish script`

`pilot-11ty` свою задачу выполнила; дальнейшая рабочая ветка — `main`, если отдельно не создаётся новая feature-ветка.

## Локальные одноразовые файлы

Локально могут оставаться untracked:

- `migrate-ru-articles.ps1`
- `migrate-uk-articles.ps1`

Это одноразовые миграционные утилиты. Не коммитить без отдельной причины.

## Что было сделано до Eleventy

Статическая версия сайта уже прошла техническую очистку:

- WordPress-архив преобразован в статический сайт;
- исправлены canonical/hreflang;
- sitemap содержит 54 URL и отправлен в Google Search Console;
- robots.txt проверен;
- внутренние ссылки нормализованы;
- большие повторяющиеся стили вынесены в `/tortopedia.css`;
- контентные изображения переведены в WebP;
- удалены ненужные AddToAny/social/Copyscape/пустые footer и sitemap-блоки;
- устранены критические битые внутренние ссылки;
- сохранена структура RU в корне и UA в `/uk/`.

Не повторять массовую косметическую очистку старого WordPress-наследия без реальной пользы. Принцип проекта — 80/20.

## Следующий этап: контент

Техническую миграцию считать завершённой.

Следующая задача — развитие контента.

Перед массовым написанием новых материалов желательно сделать редакционный style guide Tortopedia на основе существующих статей. Анализировать несколько характерных статей RU и UA и зафиксировать:

- структуру материала;
- типичные вступления и выводы;
- лексику, фразеологию и степень разговорности;
- формат H2/H3;
- использование списков, инструкций, фото и видео;
- характерные SEO-паттерны;
- особенности RU и UA отдельно.

Цель — новые статьи должны естественно продолжать старый корпус, а не выглядеть текстами из другого проекта.

## Важно не потерять: responsive images / srcset

Это отдельная незавершённая задача из предыдущего HANDOFF и её нужно сохранить в плане.

Контентные изображения уже переведены в WebP, но полноценные responsive images ещё не сделаны.

Зафиксированная стратегия:

- перед массовым внедрением `srcset` вручную улучшать наиболее ценные контентные фото: торты, мастика, фигурки, пошаговые иллюстрации;
- улучшенный большой вариант использовать как master; не гнаться за `3x`, если это требует искусственного апскейла без прироста деталей;
- целевой практический предел — примерно `1x` + до `2x` по фактическому разрешению, но в HTML НЕ использовать density-дескрипторы `1x/2x`;
- использовать `w`-дескрипторы, потому что реальные master-файлы часто имеют промежуточный запас по плотности (например около `1.66x`), а не ровно `2x`;
- браузеру передавать реальные ширины файлов через `srcset="... 600w, ... 1000w, ... 1618w"` и корректный `sizes`;
- уменьшенные версии можно генерировать автоматизированно, но финальную компрессию/визуальный контроль оставлять ручными;
- старые WordPress-суффиксы размеров в именах (`-600x350`, `-533x400` и т.п.) постепенно нормализовать при обработке конкретной статьи, не массово вслепую;
- сначала полностью отработать схему на одной типичной статье, затем масштабировать;
- убедиться, что мобильные устройства не скачивают без необходимости крупные изображения.

Текущая инвентаризация по `src`: 795 тегов `<img>`, из них 436 уже имеют `srcset`, 359 — без `srcset`; среди последних 324 уникальных `src`, у 343 тегов уже есть `width` + `height`.

Эту задачу желательно закрыть до массового добавления новых статей, чтобы новые материалы сразу создавались по правильной схеме изображений.

## Другие возможные технические задачи

Только по необходимости:

- постепенное сокращение дублирования RU/UA layouts;
- реальные разделы вместо старых пунктов меню-заглушек `#`;
- автоматическая Eleventy-сборка на Cloudflare — только если появится практическая необходимость.

Ничего из этого сейчас не блокирует работу с контентом.

## Правила безопасности

- сначала локальная сборка, затем проверка HTML, затем commit/push;
- после failed build не доверять старому `_site-pilot`;
- не применять широкие regex/whitespace-замены без проверки;
- для PowerShell-записи использовать UTF-8 без BOM;
- изменения делать test-first на одном типичном материале, затем масштабировать;
- production URL-структуру без причины не менять.
